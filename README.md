# Animal Multiclass CNN

Training and evaluation pipeline for classifying wildlife images into multiple species using a convolutional neural network. Includes data preprocessing, model architecture (MobileNet3-small), training loops with loss/accuracy tracking, and visualization of results.

## Getting Started

Clone the repository and install dependencies from requirements.txt:

```
* download the windows installer 
* place it in a desiered folder 
* double click all installs will be done automatically
* make sure you have admin rights to you machine
```

Run the app with `python Wildlife_Animal_Classifier/app.py` 
and training with `python Wildlife_Animal_Classifier/MulticlassCNN_training_final.ipynb`